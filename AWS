1. Cloud Front : - Is a content delivery network(CDN)

The primary entities of CDN include Origin, Edge Location and Distribution (is the name given to the CDN Which is a collection of edge
locations)

There are 2 distributions types in CloudFront.

First one : WEb Distribution : Which is meant for Websites.
Scond One : RTMP - Media Streaming.

  Edge Locations are not just read only - Users can also write to edge locations.
  Deletion of objects in the cache can be done but would incur charges.
